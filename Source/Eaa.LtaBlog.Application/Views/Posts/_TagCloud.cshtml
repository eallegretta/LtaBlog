@model Eaa.LtaBlog.Application.Models.TagCloudViewModel

<div id="tagCloud">
	<h3>Tags</h3>
	<ul>
		
	@foreach (var tag in Model.Tags)
	{
		<li>@Html.ActionLink(tag.Name, "List", new { controller = "Posts", tag = tag.Name, page = 1 }) (@tag.Count)</li>
	}
	</ul>
</div>